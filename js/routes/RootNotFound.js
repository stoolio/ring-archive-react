import React from 'react';

const RootNotFound = React.createClass({
  render() {
    return (
      <h2>
        There is no page by that name. Sorry!!
      </h2>
    );
  }
});

export default RootNotFound;
